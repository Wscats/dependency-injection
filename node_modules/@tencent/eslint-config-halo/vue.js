module.exports = {
  extends: ['./base.js', 'plugin:vue/essential'],
  rules: {
    'import/extensions': ['error', 'always', {
      js: 'never',
      vue: 'never',
    }],

    // 在 Vue 中使用 JSX 不需要引入 React
    'react/react-in-jsx-scope': 'off',

    // Vue 中写 JSX 会有一个未使用的 h 参数, 需要忽略
    'no-unused-vars': ['error', { argsIgnorePattern: '^h$' }],
  },
};
